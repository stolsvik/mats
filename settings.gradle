rootProject.name = 'mats'

include 'mats-api', 'mats-impl-jms', 'mats-api-test',
        'mats-serial', 'mats-serial-json',
        'mats-test', 'mats-test-junit', 'mats-test-jupiter',
        'mats-intercept-api',
        'mats-intercept-logging', 'mats-intercept-micrometer',
        'mats-spring', 'mats-spring-test', 'mats-spring-jms',
        'mats-amq', 'mats-amq-monitor',
        'mats-localinspect',
        'mats-util', 'mats-util_activemq',
        'mats-websockets'

println "** Java: ${ System.getProperty('java.version') }, Groovy: $GroovySystem.version," +
        " Gradle: ${ gradle.gradleVersion }   (printed from settings.gradle)"

